import { Button } from "@mantine/core";

const NewGameButton = () => {
  return (
    <Button variant="gradient" gradient={{ from: "yellow", to: "red", deg: 217 }}>
      New Game
    </Button>
  );
};

export default NewGameButton;
